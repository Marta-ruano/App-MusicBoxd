# App-MusicBoxd
MusicBoxd is a RESTful API built with Flask that allows users to store and review music.  It includes user authentication (sign up and login), an SQL database and supports CRUD operations with JSON responses
